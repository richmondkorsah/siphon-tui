"""Pure animation math — timing curves, per-cell transforms.

Kept Textual-free so unit tests can exercise the math without a driver.
The widget layer polls these helpers per-frame to compute the current
display state.
"""
